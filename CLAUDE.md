# draw_piper — Claude Code 運用メモ

## マイルストーン管理（MILESTONES.md）

`MILESTONES.md` は「詰まった時に戻れる正常地点」の地図。
Claude Code は以下のタイミングで **MILESTONES.md の更新を能動的に提案する**
（勝手に書き換えず、ユーザーに提案する）:

- ✅ **戻れる正常地点に到達したとき** — 機能の動作確認ができた / 実機テストが
  PASS した / 節目となるコミットをした → trunk に `●`（ポジティブ）を提案
- ❌ **作業が詰まった・失敗した・行き止まりに当たったとき**
  → branch に `✗`（ネガティブ）を提案
- 🔧 **詰まりが解決したとき** — 対処内容と「復旧先 ▶」の追記を提案

毎コミットで提案する必要はない。判断基準は **「後で戻りたくなる節目か?」**。
更新フォーマットは `MILESTONES.md` 末尾の「使い方 / メンテナンス」に従う。
