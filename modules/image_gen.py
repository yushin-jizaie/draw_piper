"""Image generation (skeleton)

Step C: will be used to test VRAM usage with SDXL Turbo and ControlNet.
"""


def generate_image(prompt, out_path):
    # placeholder
    with open(out_path, 'wb') as f:
        f.write(b'')
    return out_path
